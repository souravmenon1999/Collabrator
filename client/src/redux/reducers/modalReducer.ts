// src/redux/reducers/modalReducer.js
// This is a placeholder, replace with your actual modalReducer logic if needed
const initialState = {};

const modalReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default modalReducer;